> ci / stepsTree -v
> beforeCiSpec
> ci -v
$ must-mirror target/ci-status.html ci-status.html
$ must-mirror ci-reports/root.html root-ci-status.html
> ci / stepsTree -s

> afterCiSpec
> commandSpec
> rootSpec
> fooSpec
> barSpec
> resetScalaVersionSpec

# conflicting cross-versions between classpath deps fails ci
> clean
> set foo / crossScalaVersions := Nil
> show scalaVersion
-> ci
> conflictingCrossVersionsSpec

# compatible Scala versions should be used as fallback (like sbt does)
> clean
> set foo / crossScalaVersions := Seq("2.12.18", "2.13.11")
# for some reason the above command influences bar's crossScalaVersions too
# so we reset bar to original
> set bar / crossScalaVersions := Seq("2.12.20", "2.13.16")
> ci -v
> fooSpec
> barSpec

## aggregate setting should be honored in commands
> clean
> set foo / ci / steps := Nil
> set bar / ci / steps := Nil
> set ci / steps := "compileCommand" :: Nil
> ci
> aggregateCommandSpec
# test short option ordering (-vs -sv etc)
> ci / stepsTree -vs
